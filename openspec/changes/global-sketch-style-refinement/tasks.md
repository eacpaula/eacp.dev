## 1. Refine the shared visual source of truth

- [x] 1.1 Update `DESIGN.md` to document the sketch-inspired background mood, palette refinement, texture rules, surface language, and whole-site reuse guidance.
- [x] 1.2 Refine the global token and background layer in `src/styles/globals.css` so the page atmosphere feels more charcoal, graphite, and concept-art aligned without adding noisy or heavy texture.

## 2. Update shared surfaces and hero-adjacent styling

- [x] 2.1 Adjust shared layout and UI surface patterns so borders, panel separation, and depth cues feel less sterile and more integrated with the portrait’s sketch-inspired world.
- [x] 2.2 Refine the hero shell and surrounding global background so the portrait panel and right-side content area feel visually unified.

## 3. Apply the system across representative sections

- [x] 3.1 Update representative section surfaces in `src/components/sections/ProfessionalSummarySection.tsx`, `EngineeringImpactSection.tsx`, `SkillsSection.tsx`, and `ExperienceSection.tsx` to match the refined charcoal-and-ink material language.
- [x] 3.2 Update supporting sections in `src/components/sections/CaseStudiesSection.tsx`, `BlogPreviewSection.tsx`, and `ContactSection.tsx` so the refined visual system scales consistently through the full homepage.

## 4. Validate consistency and usability

- [x] 4.1 Run a desktop and mobile visual review to confirm the new background and surfaces feel cohesive with the hero portrait while preserving readability.
- [x] 4.2 Verify the final styling remains monochrome-first with restrained gold accents, avoids startup-like gradients or clutter, and remains realistic for a modern frontend implementation.
